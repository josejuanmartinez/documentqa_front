export {}
/*type Features = {
    key: string;
    value: boolean;
};

export default function setFeature(key:string, value:boolean, features:Features[]): Features[] {
    console.log(features);
    let copy: Features[] = features;
    copy.filter(x => x.key==key).forEach((x: Features) => {x.value = value});
    return copy;
}*/