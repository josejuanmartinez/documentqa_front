export {}
/*
type Features = {
    key: string;
    value: boolean;
};

export default function getFeature(key:string, features:Features[]): boolean {
    const res = features.filter(x => x.key==key)
    if (res.length>0) {
        console.log(res[0].value);
        return res[0].value;
    } else {
        console.log("Trying to access " + key + " but not found in the features context. Returning false.");
        return false;
    }
}*/
